package cs3500.animator.provider.model;

/**
 * This enumeration is used to create shapes, and are the shape types that are returned. This is to
 * ensure the user explicitly knows what shape types are supporte. (And also desn't typo when making
 * their shapes in the {@code EasyAnimator}.
 */
public enum Shapes {
  RECTANGLE, ELLIPSE
}
